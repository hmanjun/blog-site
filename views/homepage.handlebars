{{#each posts as |post|}}
<div class="post-container bg-secondary" data-post_id="{{post.id}}">
    <h2>
        {{post.title}}
    </h2>
    <h3>
        Created by {{post.user.username}} on {{post.createdAt}}
    </h3>
</div>
{{/each}}